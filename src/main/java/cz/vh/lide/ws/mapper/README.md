# ws.mapper

Mapping between ws.dto DTOs and db.dto DTOs/entities.

## Mappers
- WsMapper: API-layer mapping component used by controllers.

## Public interface pattern
- toDbDto(apiDto) / toApiDto(dbDto) mappings.