DB_URL=jdbc:postgresql://localhost:5432/db_lide
DB_USER=app_lide
DB_PASS=change_me
## Optional: CORS allowed origins (comma separated). For production set to your frontend URL(s), e.g.
# app.cors.allowed-origins=http://app.example.com,https://app.example.com

## APP_PORT: default port for the backend (optional)
# APP_PORT=8081
